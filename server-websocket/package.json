{
	"name": "server-websocket",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node index.js",
		"docker-build": "docker build -t server-websocket .",
		"docker-run": "docker run -p 4000:4000 server-websocket"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^5.1.0",
		"ws": "^8.18.2"
	}
}
